@model IEnumerable<Pmat_PI.Models.TipoEscola>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoEscola1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoTipoEscola)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoEscola1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoTipoEscola)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoEscola">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoEscola">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoEscola">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
